apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: external-secrets-crds
  namespace: external-secrets
spec:
  # Stage 2: the keep policy is recorded in release revision 2 and all CRDs
  # are Established. Freeze this release while the controller takes ownership.
  suspend: true
  interval: 30m
  releaseName: external-secrets-crds
  driftDetection:
    mode: enabled
  chart:
    spec:
      chart: external-secrets
      version: 2.6.0
      sourceRef:
        kind: HelmRepository
        name: external-secrets
        namespace: flux-system
      interval: 24h
  install:
    crds: CreateReplace
    remediation:
      retries: 3
  upgrade:
    crds: CreateReplace
    cleanupOnFail: true
    remediation:
      retries: 3
      strategy: rollback
  values:
    installCRDs: true
    createOperator: false
    webhook:
      create: false
    certController:
      create: false
    rbac:
      create: false
    serviceAccount:
      create: false
    crds:
      annotations:
        # Persist this in the CRD release manifest before the later ownership
        # hand-off, so retiring this release cannot delete live CRDs.
        helm.sh/resource-policy: keep
      createClusterExternalSecret: false
      createClusterSecretStore: true
      createSecretStore: false
      createClusterGenerator: false
      createClusterPushSecret: false
      createPushSecret: false
    processClusterExternalSecret: false
    processClusterStore: false
    processSecretStore: false
    processClusterGenerator: false
    processClusterPushSecret: false
    processPushSecret: false
